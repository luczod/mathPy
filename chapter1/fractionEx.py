from fractions import Fraction

f = Fraction(3, 5)
f2 = Fraction(3, 4) + 1 + Fraction(1 / 4)

# When we print the object w, Python displays the fraction in the
# form Fraction(numerator, denominator).
print(f2)
